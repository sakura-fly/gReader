#  项目开发指南

## 构建运行

```bash
mvn clean compile          # 编译
```

## 特殊规则

1. **用汉语思考和回应**：所有思考过程，对话，任务说明和用户交互使用汉语。
2. **生成字段描述**：所有字段的描述，请使用中文。
3. 代码关键部分也要添加注释。
4. **新生成的代码文件自动 git add**：创建新文件后执行 `git add <file>` 将其加入暂存区。
